Group `body`, `eye-left`, and `eye-right`, in that order, under a new group with
ID `mascot`. Move the group by transform `translate(24 12)` without changing
any child properties. Preserve `frame` exactly. Return the complete SVG.
